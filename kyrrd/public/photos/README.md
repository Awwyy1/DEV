# Drop real photos here

Put image files in this folder, then reference them from `src/plates.ts`:

    image: '/photos/your-file.jpg'   // overrides the gradient placeholder

You can also paste any external URL into the `image` field instead.
Recommended ratio 4:5 (portrait). Folder served at the site root by Vite.
